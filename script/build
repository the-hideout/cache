#!/bin/bash

set -e

CRYSTAL_FILE=cache

mkdir -p dist/

echo "Building $CRYSTAL_FILE in release mode"
crystal build "src/$CRYSTAL_FILE.cr" -o "dist/$CRYSTAL_FILE" --release --progress
echo "📦 Done building $CRYSTAL_FILE"
