#!/bin/bash

set -e

# set the working directory to the root of the project
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )"

echo "🔨 building in release mode"
SHARDS_CACHE_PATH="$DIR/.cache/shards" shards build --release --progress --debug
echo "📦 build complete"
