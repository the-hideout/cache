#!/usr/bin/env bash

set -euo pipefail

source script/env "$@"

echo -e "${BLUE}Entering cache directory...${OFF}"
cd "$CACHE_GO_DIR"

echo -e "${BLUE}Verifying vendored Go dependency graph...${OFF}"
go list -mod=vendor -deps ./... >/dev/null

echo -e "${GREEN}✅ Bootstrap complete!${OFF}"
